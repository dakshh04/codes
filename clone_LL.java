class Node{
    int data;
    Node next, random;
    Node(int x){
        data = x;
        next = random = null;
    }
}

public class clone_LL {
    static Node clone(Node head){
        Node next, temp;

        for(Node curr = head; curr!=null; curr)
    }
}
